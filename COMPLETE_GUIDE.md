# 📱 FWP AudioChat - সম্পূর্ণ সিস্টেম গাইড

## ✅ সিস্টেম সেটআপ সম্পূর্ণ!

### 🔧 সার্ভার স্ট্যাটাস
- ✅ **ব্যাকএন্ড**: http://localhost:5000
- ✅ **ফ্রন্টএন্ড**: http://localhost:3000
- ✅ **ডাটাবেজ**: In-Memory (Development)

---

## 📋 কীভাবে ব্যবহার করবেন

### 1️⃣ প্রথমবার সেটআপ
```bash
# ব্যাকএন্ড শুরু করুন (টার্মিনাল ১)
cd C:\Users\User\social_chat_app\backend
node server-dev.js

# ফ্রন্টএন্ড শুরু করুন (টার্মিনাল ২)
cd C:\Users\User\social_chat_app\frontend
$env:NODE_OPTIONS='--localstorage-file=.localstorage'
$env:REACT_APP_API_URL='http://localhost:5000/api'
npm start
```

### 2️⃣ নতুন অ্যাকাউন্ট তৈরি করুন
1. http://localhost:3000/signup এ যান
2. নাম, ইমেইল/ফোন, পাসওয়ার্ড দিন
3. "Create Account" ক্লিক করুন
4. ✅ অটোমেটিক ইউনিক আইডি তৈরি হবে (FWP...)

### 3️⃣ লগইন করুন
1. http://localhost:3000/login এ যান
2. ইমেইল/ফোন এবং পাসওয়ার্ড দিন
3. "Login" ক্লিক করুন
4. ✅ সফলভাবে লগইন হবেন

### 4️⃣ প্রোফাইল দেখুন
- http://localhost:3000/profile এ যান
- আপনার সমস্ত তথ্য দেখবেন
- ⚙️ সেটিংস ড্রয়ার খুলুন (ডান দিকে)

---

## 🔐 অনলাইনে ডিপ্লয় করার জন্য (পরবর্তীতে)

### MongoDB Atlas সেটআপ
1. https://www.mongodb.com/cloud/atlas এ যান
2. বিনামূল্যে ক্লাস্টার তৈরি করুন
3. Connection String কপি করুন
4. `.env` তে যোগ করুন:
```env
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/fwp_audiochat?retryWrites=true&w=majority
```

### Cloudinary সেটআপ
1. https://cloudinary.com এ যান
2. একাউন্ট তৈরি করুন
3. API Key গুলো কপি করুন
4. `.env` তে যোগ করুন:
```env
CLOUDINARY_CLOUD_NAME=xxx
CLOUDINARY_API_KEY=xxx
CLOUDINARY_API_SECRET=xxx
```

---

## 📱 APK/অ্যাপ তৈরি করুন

### Android APK তৈরি করুন
```bash
cd C:\Users\User\social_chat_app\frontend
npm run build
```
✅ APK হবে: `C:\Users\User\Downloads\FWP-AudioChat-Mobile.apk`

### Windows Desktop App তৈরি করুন
```bash
npm run electron-build
```
✅ EXE হবে: `C:\Users\User\social_chat_app\frontend\dist`

---

## 🎯 API Endpoints

### Authentication
- `POST /api/auth/signup` - নতুন অ্যাকাউন্ট তৈরি
- `POST /api/auth/login` - লগইন
- `GET /api/auth/me` - বর্তমান ইউজার
- `PUT /api/auth/profile` - প্রোফাইল আপডেট
- `PUT /api/auth/change-password` - পাসওয়ার্ড পরিবর্তন
- `PUT /api/auth/change-email` - ইমেইল পরিবর্তন
- `PUT /api/auth/change-phone` - ফোন পরিবর্তন

### Health Check
- `GET /api/health` - সার্ভার স্ট্যাটাস

---

## 🔒 ডেটা কী ঘটে?

### ব্যবহারকারীর ডেটা সংরক্ষণ
✅ **সার্ভারে সংরক্ষিত:**
- ইউনিক আইডি (FWP...)
- নাম, ইমেইল, ফোন
- হ্যাশড পাসওয়ার্ড (bcrypt)
- প্রোফাইল তথ্য
- লগইন ইতিহাস

### নিরাপত্তা
- ✅ পাসওয়ার্ড এনক্রিপ্টেড (bcrypt)
- ✅ JWT টোকেন ব্যবহার
- ✅ CORS সুরক্ষা
- ✅ HTTPS (প্রোডাকশনে)

---

## ⚠️ গুরুত্বপূর্ণ নোট

### বর্তমান পর্যায়ে (Development)
- ডেটা **মেমোরিতে** সংরক্ষিত
- অ্যাপ বন্ধ হলে সমস্ত ডেটা মুছে যাবে
- শুধুমাত্র পরীক্ষার জন্য ব্যবহার করুন

### প্রোডাকশনের জন্য
- MongoDB Atlas ব্যবহার করুন
- Cloudinary সংযোগ করুন
- Environment variables সঠিকভাবে সেট করুন
- SSL সার্টিফিকেট যোগ করুন

---

## 🆘 সমস্যা সমাধান

### Port ইতিমধ্যে ব্যবহার করা হচ্ছে
```bash
# সমস্ত Node প্রসেস বন্ধ করুন
taskkill /F /IM node.exe
```

### API সংযোগ ব্যর্থ
1. ব্যাকএন্ড সার্ভার চলছে কিনা চেক করুন
2. `http://localhost:5000/api/health` ভিজিট করুন
3. ফ্রন্টএন্ডে environment variable চেক করুন

### Frontend কমপাইল ব্যর্থ
```bash
cd C:\Users\User\social_chat_app\frontend
rm -r node_modules\.cache
npm start
```

---

## 📞 যোগাযোগ

সমস্যা হলে জানান - সব ঠিক করে দেব! ✅

